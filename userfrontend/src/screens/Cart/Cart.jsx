import {useContext} from 'react'
import {StoreContext} from '../../context/StoreContext'

const Cart = () => {
  return (
    <div className='cart'>
      const{cartItems,food_list}
    </div>
  )
}

export default Cart